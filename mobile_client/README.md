# AI财 - 智能AI助手

你的互联网嘴替兼顶级狗腿子！基于 Flutter 的多功能 AI 助手客户端。

## 特性
- 🤖 智能 Agent 推理链与多步计划执行
- 🔍 多源网络搜索（Exa.ai, You.com, Brave）
- 📚 本地知识库管理
- 🎨 AI 绘图支持
- 📱 系统控制（需开启无障碍服务）
- 🧠 用户画像记忆
- 💬 多人格切换

## 准备
1. 安装 Flutter 3.x（含 Android SDK/模拟器），配置好 `flutter doctor`。
2. 在本仓根目录运行：
   ```bash
   flutter create aicai_assistant
   ```
   然后用本目录的文件覆盖生成的同名文件。

## 运行与打包
```bash
cd mobile_client
flutter pub get
./configure_android.sh  # 配置 Android 项目（权限、包名等）
flutter run              # 连接真机或模拟器调试
flutter build apk        # 生成 release APK
```

## 配置
在设置页面配置：
- **聊天 API**: OpenAI 兼容的 API 地址和密钥
- **搜索 API**: Exa.ai / You.com / Brave Search 密钥
- **绘图 API**: DALL-E 或兼容 API
- **识图 API**: GPT-4 Vision 或兼容 API

## 注意
- 新版本与旧版本使用不同包名，可以共存
- 包名: `com.aicai.app.aicai_assistant`
